#!/usr/bin/env bash

mkdir -p docs

xcodebuild docbuild \
  -scheme NavigationTransitions \
  -destination generic/platform=iOS \
  OTHER_DOCC_FLAGS="--transform-for-static-hosting --hosting-base-path /swiftui-navigation-transitions/$1 --output-path docs/$1"
